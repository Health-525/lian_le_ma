# App 环境变量示例（复制为 .env 后填写）。Expo 以 EXPO_PUBLIC_ 前缀暴露给客户端。

# 模型服务（web_app.py）基址，默认监听 4000 端口。
# 留空 = 演示模式（本地桩）；填写 = 直连真模型做姿势矫正。
# 真机调试时填模型机器在局域网中的 IP，例如 http://10.160.0.120:4000
EXPO_PUBLIC_MODEL_BASE_URL=
# 识别模式：manual=按所选动作评估（默认）；auto=模型自动识别动作。
EXPO_PUBLIC_MODEL_MODE=manual
